import React from "react";

const SelectedNumber = ({ specifiedNumber }) => {
    return (
        <div>
            <div>{specifiedNumber}</div>
        </div>
    );
};

export default SelectedNumber;
